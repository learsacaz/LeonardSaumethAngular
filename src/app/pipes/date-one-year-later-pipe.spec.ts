import { DateOneYearLaterPipe } from './date-one-year-later-pipe';

describe('DateOneYearLaterPipe', () => {
  it('create an instance', () => {
    const pipe = new DateOneYearLaterPipe();
    expect(pipe).toBeTruthy();
  });
});
